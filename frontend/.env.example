# --- Supabase (required) ---
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-side key (recommended for secure server routes, webhooks, admin operations)
SUPABASE_SERVICE_ROLE_KEY=

# --- Integration proxy allowlist (optional) ---
# Comma-separated domains that /api/integrations/proxy will allow
INTEGRATION_PROXY_ALLOWLIST=api.example.com

# --- App ---
APP_NAME=