export default function Page() {
  return (
    <main
      style={{
        minHeight: "calc(100vh - 3rem)",
        padding: "2rem 1.5rem",
        display: "flex",
        flexDirection: "column",
        gap: "0.75rem",
      }}
    >
      <h1 style={{ fontSize: "1.4rem", margin: 0 }}>{$Title}</h1>
      <p style={{ margin: 0, fontSize: "0.95rem", opacity: 0.85 }}>
        {$Body}
      </p>
    </main>
  );
}